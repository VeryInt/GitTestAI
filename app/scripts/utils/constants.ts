const baseClassName = `git-test-ai`
export const GTAVirtualRootId = `${baseClassName}-virtual-root`
export const GTAClassNames = {
    base: baseClassName,
    root: `${baseClassName}-virtual-root`,
}